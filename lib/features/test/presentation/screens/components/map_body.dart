import 'package:flutter/material.dart';

class MapBody extends StatelessWidget {
  const MapBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('there you go'),);
  }
}
